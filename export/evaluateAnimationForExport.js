/**
 * Evaluates animation timeline for export purposes.
 *
 * @param {Object} params
 * @param {Object} params.animations
 *
 * @returns {Object} export-ready animation model
 *
 * Rules:
 * - Stateless
 * - Deterministic
 * - Does NOT use preview output
 */
export function evaluateAnimationForExport({ animations }) {
  void animations;
  return {};
}
