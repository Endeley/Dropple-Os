export const OS_WORKSPACE_SHELL_ACTION_POLICY_VERSION = '1';

export const OS_WORKSPACE_SHELL_ALLOWED_ACTIONS = Object.freeze([
    'workspace.activate',
    'mode.activate',
    'tool.activate',
    'viewport.set',
]);
