'use client';

/**
 * BehaviorPreviewLayer
 *
 * Visual-only preview layer for behavior state transitions.
 * Must not mutate canonical state.
 */
export function BehaviorPreviewLayer() {
    return null;
}

