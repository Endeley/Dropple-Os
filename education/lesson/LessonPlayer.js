export class LessonPlayer {
  constructor({ lesson, container }) {
    this.lesson = lesson;
    this.container = container;
  }

  play() {
    // Initialize WorkspaceShell in observer + education mode
    // Load lesson.timeline.events
    // Attach AnnotationOverlay
    // Auto-advance cursor
  }
}
