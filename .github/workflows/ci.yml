name: CI

on:
  push:
  pull_request:

jobs:
  guards:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: CI Guards
        run: |
          scripts/ci/check-ux-phase-5-guardrails.sh
          scripts/ci/check-no-pre-dispatch-event-ids.sh
          scripts/ci/check-single-message-bus.sh
          scripts/ci/check-ccm-validator-sync.sh
