/**
 * Deterministic scene evaluation entrypoint.
 * Currently returns null until scene evaluation is implemented.
 */
export function evaluateScene(timeMs) {
    if (!Number.isFinite(timeMs)) return null;
    return null;
}
